# Default is mock, so the app works without a paid AI API.
AI_PROVIDER=mock

# Use this later for local free/open-source models:
# AI_PROVIDER=ollama
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=qwen3:8b
